using Assets.src.items;
using UnityEngine;

public class GameActor : MonoBehaviour
{
    // Start is called before the first frame update

    public Weapon Weapon;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
